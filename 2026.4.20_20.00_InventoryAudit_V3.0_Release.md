# Release Notes: Inventory Audit Module V3.0 (2026-04-20)

## MỤC TIÊU KIẾN TRÚC
Phiên bản V3.0 là một cuộc đại tu kiến trúc gốc của mô-đun Kiểm Kê Hàng Loạt (Mass Inventory Audit). Thay vì là một "Pop-up" đơn giản, module này hiện tại là một **Ứng dụng Single-page con (Mini SPA)** chạy song song trên trang chính, được thiết kế chuyên biệt cho môi trường xưởng với các tác vụ quét súng QR nhịp độ cao, offline-first và bảo vệ toàn vẹn dữ liệu.

## TÍNH NĂNG ĐÃ TRIỂN KHAI

### 1. Kiến trúc Offline-First (LocalStorage Engine)
* **Phiên Kiểm Kê Di Động (Sessions):** Mỗi đợt kiểm kê được tạo thành một "Session" độc lập với một ID ngẫu nhiên, lưu vào `localStorage`. Điều này cho phép user có thể đóng cửa sổ / refresh trang mà không mất dữ liệu của các mã hàng đã quét.
* **Delta Sync Protocol:** Thay vì đẩy API theo từng dòng (gây Request Flood) hoặc đẩy cục bộ một lần, hệ thống sử dụng cờ `synced: false/true`. Khi user ấn nút "Đồng Bộ", chỉ các mã chưa từng đồng bộ mới gộp thành Payload gửi đi.
* **Quota Management Rescue:** Phát hiện Uncaught QuotaExceededError khi LocalStorage > 5MB, tự động giải phóng vùng nhớ `audit_session_history` cũ để bảo toàn tính năng Offline.

### 2. Trải Nghiệm Người Dùng (UI/UX) Layout Song Hành
* **Desktop Layout:** Giao diện truyền thống chia 2 Panel (Left - Bảng điều khiển / Right - Danh sách chờ quét).
* **Mobile Layout (Xưởng):** Giao diện sập Fullscreen che khuất background. Dùng hệ thống 3 Tabs ("入力 - Nhập", "リスト - Danh Sách", "完了 - Đã Điểm") tối ưu cho thao tác 1 ngón tay trên súng quét di động.
* **Quy tắc Đa Ngôn Ngữ:** 100% Giao diện được cấu trúc theo chuẩn Nhật-Việt (Tiếng Nhật chiếm kích cỡ chính).

### 3. Safety Locks (Hàng Rào An Toàn)
* Nhánh phòng thủ: Check list > 2000 thiết bị sẽ bật Confimation chặn người dùng khỏi việc ấn nhầm (VD rò rỉ danh sách lọc).
* Detect Loại Thiết Bị: Tự động switch ngữ cảnh giữa Khuôn (Mold) và Dao Cắt (Cutter) dựa vào danh sách `window.app.filteredItems`, user không cần tự chọn Mode.

### 4. Nâng Cấp UTF-8 / Định dạng Data
* Payload ném về API `POST: /api/audit-batch` ép Header `application/json; charset=utf-8` để lưu sessionName bằng tiếng Việt / tiếng Nhật không bị hỏng bảng mã trên backend.

## THIẾT KẾ CẤU TRÚC KIỂM KÊ
* Module được viết trong `inventory-audit-module.js` với Event-Driven Object.
* Dữ liệu hiện mới cover Mode 1 (Checklist - Target Racks), mã quét đã xử lý regex Prefix (`YSD_MOLD:`, `YSD_CUT:`).
* Các biến *Reserved Fields* như `expectedRackLayer`, `relocated` và format QR Regex `YSD_RL:` đã được cài đặt sẵn để chuẩn bị cho Phase 4 (Tính năng Chuyển Kệ Tức Thì khi sai vị trí).

## TÀI LIỆU LIÊN QUAN
Các chi tiết cấu trúc JSON Session, tham khảo Artifact: `inventory-audit-module-v8.5.3.md`.
