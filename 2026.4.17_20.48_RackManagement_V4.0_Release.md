# Phiên Làm Việc: Global History Sidebar Module & Rack Management
**Dự án:** MoldCutterSearch
**Module:** Hệ thống giám sát lịch sử (Global History) & Rack Management

## 1. Giao diện (UI) và Tích hợp
- Đã chỉnh sửa thẻ `<aside id="sidebar">` bên trong file `index.html` để gán ID `sidebarHistoryModuleBtn` cho nút "Lịch sử".
- Đã thêm thẻ Node Notification Badge (kiểu viền đỏ Facebook) ẩn bên cạnh icon Lịch sử để hiển thị số lượng thay đổi chưa đọc.
- Bố trí thẻ chứa nội dung chính `id="mcs-view-history"` trong `<main class="main-content">` để đóng gói container view chuyên biệt cho Lịch sử.
- Load file module JS (`global-history-module.js`) và CSS (`global-history-module.css`) tại phần đuôi trang.

## 2. Logic Hệ Thống & Xử Lý Sự Kiện (JS)
- Thiết lập **GlobalHistoryModule** bắt trọn mọi data ngay sau khi `window.DataManager` khởi tạo hoàn tất hoặc khi event `mcs-data-loaded` được kích hoạt.
- Tự động gom 5 mảng dữ liệu phân tán (`statusData`, `itemLocationData`, `shipData`, `teflonData`, `commentsData`) thành một trục thời gian (Timeline) duy nhất, sắp xếp giảm dần theo Milliseconds.
- Xây dựng Data Table siêu gọn (< 500 records tại DOM để tăng tốc độ lướt di động), tích hợp thanh công cụ tìm kiếm tức thời theo cụm từ: Mã, Người thực hiện, Ghi chú.

## 3. Biểu Tượng Thông Báo (Facebook Notify)
- Hệ thống duy trì biến trạng thái `mcs_history_last_checked` chứa Millisecond cuối cùng mở tab Lịch sử tại LocalStorage.
- So sánh các bản ghi được phát hành trên Timeline có `timeMs` lớn hơn con số nói trên sẽ tăng số đếm (Badge Counter). Hệ thống hiển thị tối đa `99+`.
- Khi người dùng click vào sidebar "Lịch sử", đồng hồ tính giờ dời về mức hiện tại để làm trắng thông báo.

## 4. Chức Năng Xuất Dữ kết
- Hệ thống tận dụng engine `ExcelJS` tích hợp sẵn trong máy nền tảng để cho phép xuất tức thì Bảng lịch sử ra tệp .xlsx Native theo đúng từ khóa tìm kiếm đang áp dụng (Filter Output).
- Hỗ trợ dàn trang layout HTML In Ấn trực diện (Print).

## 5. Cập Nhật Bugfix V8.1.0-4
- **Lỗi giao diện (Layout / CSS):** Đã tinh chỉnh khoảng cách cột bảng lịch sử bằng CSS `nowrap`, khống chế độ rộng cột Tên Nhân Viên bằng Text-ellipsis để các dòng gọn gàng, tránh ngập/cắt chữ khó đọc.
- **Dịch Thuật Hành Động:** Toàn bộ log như Vận Chuyển (`Ship`), Nhập/Xuất (`Status IN/OUT`) đã được đổi sang tiếng Anh chuẩn để đồng nhất toàn cầu theo yêu cầu.
- **Người Thao Tác:** Lỗi thông tin trống (-) đã được fix. Hệ thống móc mảng Global `window.DataManager.data.employees` và phân tích chéo ID để phiên dịch thành chuỗi Tiếng Nhật (Tên Nhân Viên) đầy đủ.
- **Xung Đột View (Overlap):** Kích hoạt bộ thu nhận luồng sự kiện Native `mcsViewChanged`. Khi chuyển sang Tab Nhựa / Khay qua Menu chính lẫn Floating Widget, tab Lịch Sử sẽ lập tức bị giấu đi, trả màn hình sạch cho View mới.
- **Tương Tác Thông Báo (Interactive Read State):** Lập trình lại cơ chế hiển thị badge notification giả lập hành vi iOS. Các sự kiện mới xuất hiện sẽ được tô viền nền vàng nhạt có thể bấm vào (clickable). Số lượng trên Badge Sidebar đếm đúng số kiện chưa đọc. Người dùng click vào dòng nào, nền vàng tắt và số đếm tụt xuống.
- **Cải tiến Layout Toàn Cục:** Đã giảm font-size tổng thể xuống 90% (`font-size: 90%`) và độ giãn cách padding trên file `global-history-module.css` để tăng tối đa số bản ghi (data density). Bổ sung Rule `@media` cho Responsive Mobile: Khi trên điện thoại (dưới 768px), các dòng chữ dài sẽ tự động xuống dòng (`white-space: normal`, `word-break: break-word`) thay vì bị cắt xén (cut-off) bởi `text-overflow`.
- **Thiết Kế Đa Ngôn Ngữ (Bilingual Dashboard):** Áp dụng thiết kế tiêu đề kép như bảng Plastic WMS: Nhật ngữ lớn nổi bật, Việt ngữ nhỏ xám đặt ngay dưới. Bổ sung liên kết mở trực tiếp **Detail Panel** khi click vào mã khuôn/dao. Tái lập quy chuẩn dịch toàn bộ tag Hành Động (Action) sang tiếng Nhật truyền thống.
- **Tương Tác Layout Dữ Liệu Lớn (Data Grid Capabilities):** Cấu trúc lại bảng tiêu chuẩn y hệnh bảng tìm kiếm chính. Cột và vùng dữ liệu đều được **kẻ dòng** (`border: 1px solid #e2e8f0;`) gọn gàng chuyên nghiệp. Tiêu đề các cột giờ đây có thể bấm vào để **sắp xếp bảng động (Dynamic Sorting)**, và mọi cột đều hỗ trợ kéo thả chuột góc bên phải để **đổi chiều rộng tự do (Draggable column-resize)**.
- **Tính Năng Assistive Touch (Menu Nổi):** Thay thế bảng Chuyển Module (Module Switcher) thô cứng bằng một **Nút Tiện Ích Tròn (Floating Action Button)**. Popup sẽ xổ ra dạng hình ảnh trực quan khi bấm.
  - **Trên Mobile:** Nút này được ẩn hoàn toàn để không cản trở diện tích theo dõi lịch sử.
  - **Trên Desktop:** Nút được chèn khả năng kéo thả (Drag & Drop), nhân viên có thể thoải mái nhấc nút kéo sang bất kỳ cạnh góc nào nếu lỡ vướng khung nhìn bảng biểu.

## 6. Teflon Module (Sidebar Integration)
- Xây dựng **GlobalTeflonModule** (`global-teflon-module.js` & `global-teflon-module.css`) dựa trên kiến trúc Native View (giống Module Lịch sử) thay vì dùng Modal (Cửa sổ nổi bị che khuất).
  - **Liên kết Panel Chi Tiết:** Gắn `onclick="window.DetailPanel.open()"` và truyền đối tượng tĩnh vào tên khuôn để mở cửa sổ chi tiết an toàn.
  - **Màu thông báo Sidebar:** Tùy biến linh hoạt icon `nav-badge`. Nếu có khuôn đã được duyệt (Approved) thì icon sẽ nháy ĐỎ với tổng số khuôn đang xử lý. Nếu chỉ có khuôn đang chờ duyệt (Pending), sẽ báo màu CAM.
  - **Đồng bộ Global Searchbox:** Sửa text placeholder cho thanh search trên cùng thành `型番、備考などを検索…` (Tìm mã khuôn, ghi chú). Tích hợp event listener tự động lọc bảng Teflon từ khóa gõ vào ô search Topbar khi đang đứng tại View Teflon.
  - **Giảm tải Mật độ Header:** Lược bỏ hoàn toàn khối `.gt-header` cồng kềnh phía trên để trả lại không gian tối đa cho Layout như module Plastic WMS; di dời các nút Refresh ngang hàng với thanh Metric KPIs. Bảng giờ đây rộng rãi và đẹp hơn.
  - Sửa lỗi hiển thị UI `.nav-badge` bị cắt nát (clipped) khi thanh sidebar thu gọn (`collapsed`) thông qua việc overwrite `right/top` properties. Cải thiện triệt để rendering Pagination để xử lý mượt mà hơn 4500 dòng khuôn.
- **Micro-Interactions & Bugs Triage:**
  - Sửa lỗi chữ đánh vào ô tìm kiếm Global bị khuất khi dùng pill "TEF" thông qua CSS offset. Đã cấu hình nút `Clear Search (x)` tự xóa kết quả bảng Teflon.
  - Sửa lỗi Tiêu đề bảng bung 3 dòng xấu xí nhờ thay thế cấu trúc thẻ bọc nội tuyến `inline-block`. Rút gọn "Xuất nhập kho" khỏi Sidebar vì đã trùng lặp chức năng.
  - **Splitting History Badges**: Đã tách badge `Move` trong trang Hệ thống Lịch sử thành 2 thẻ **Location** (sự kiện dịch chuyển gốc) và **Ship** (sự kiện vận chuyển qua lại giữa các xưởng) độc lập.
  - **Sidebar Module Restructure**: Tư duy thiết kế UX đã phân mảnh cấu trúc Sidebar thành 3 khu vực:
     - **MAIN**: Duy nhất tính năng cốt lõi là *Tìm Kiếm Khuôn & Dao cắt*. Mục quản lý Dao cắt trống bị lược bỏ.
     - **WAREHOUSE**: Chứa toàn vẹn flow logistic trong kho bãi: *Lịch sử, Vị trí, Khay, Nhựa (WMS), và Theo dõi mạ Teflon*. Teflon không bị lu mờ độc lập mà bám theo chu trình mạ của khuôn vận hành. 
     - **TOOLS**: Vùng rìa cho các *Công cụ Kiểm kê, Upload/Quản lý Hình ảnh*, và *Reset Data* nguy hiểm bị chôn vùi xuống đáy chống thao tác lỗi hàng ngày.
  - **Location UI Migration:** Thay thế Modal (Popup) chứa công cụ Quản lý Vị Trí / Giá Kệ cũ thành cấu trúc **Full Page Dashboard**. Cấu trúc mới được thừa hưởng dãy biểu tượng KPI theo dõi tổng số Giá Kệ và Tầng hiện hành. Thanh tìm kiếm cũng được nâng cấp để có thể làm ngơ khoảng trắng và dấu nối (`-`) y hệt như History Module. Khả năng click thẳng ở List dọc được tối ưu cho PC & iPad. Đã Fix lỗi view trống bằng cách Registry Component vào hệ thống ViewManager engine. Cũng như fix lỗi thẻ Comment HTML block mất luồng render của JS engine (`initDOM()`), làm trang trắng tinh.
  - **Location Name Overhaul:** Tiến hành tái định vị (re-branding) thuật ngữ từ "Quản lý Vị trí (Location)" tản mạn thành **Quản lý Giá kệ (ラック管理 / 保管ラック管理)** trực tiếp và phổ quát với ngành kho xưởng hơn. Toàn bộ Icon được chuyển từ bản đồ sang biểu tượng Giá (Layers `fa-layer-group`).

## 7. Xác nhận chất lượng mã
- Dữ liệu mã thông dịch tuân thủ chuẩn tiếng Nhật, tuân thủ Ruleset UTF-8 Không dùng BOM.
- Blueprint `MoldCutterSearch_MASTER.md` đã được update dòng thời gian hoàn thiện Module này.

## 8. High-Density UI & Tính Năng Chuyên Sâu Rack Management (V3.0)
- **Chuyển Search lên Top Bar Toàn Cục**: Giấu đi các field tìm kiếm cục bộ chiếm diện tích, đoạt quyền thanh Search trên cùng để tăng không gian làm việc. Kèm đó là nút List Picker cũng bay lên thanh công cụ trên cùng.
- **Table là View mặc định**: Đặt `currentViewMode = 'table'` giúp dễ nhìn lượng lớn khuôn. Khắc phục triệt để lỗi "Card View bị trống" cho Rack bằng cách tiêm Type Mold vào item.
- **CSS Mật độ cao (High-Density)**: Nhúng lớp `locm-v2-density-css` ép margin và padding nội dung bảng xuống mức ngậm ngùi nhất để giống Layout giám sát màn hình lớn.
- **In Ấn / Xuất File Excel CSV Siêu Chuẩn**: Bổ sung module xuất Excel nhóm theo Alphabet trong các Tầng. Hoàn thiện song ngữ Việt/Nhật.

## 9. Nâng cấp Navigation Cấp Độ Sâu (Deep Jump) & Dashboard Trang Chủ (V3.1)
- **Trình Xem Ảnh Rack Bản Gốc (Click 2 State) + Lọc lỗi chưa Thumbnail**: 
   - Vĩnh biệt lỗi tải ảnh xong không thấy. Thay vì gọi `getThumbnailUrl` ngây thơ, hệ thống được trang bị bộ tóm `fetchActivePhoto`. Nếu ảnh quên không bật Cờ (is_thumbnail), bot sẽ chạy `listForDevice` lùng sục lấy ảnh Active mới nhất để bảo kê hiển thị.
   - Khi bấm, ảnh hiển thị giữa màn hình với kích thước Viewport trùm lớn, kèm hiệu ứng Dim. Bấm cái nữa (hoặc Drag) sẽ chọc vào ảnh HD có kích thước Thật nguyên xi không crop. Lỗi vỡ ảnh khi Zoom bị thanh lý.
- **Dashboard Lưới (Trang Chủ Rỗng)**: Biến giao diện Rỗng vô hồn của Rack System thành Bảng Điều Khiển Mạng Nhện. Người dùng vào View chưa chọn Kệ sẽ thấy la liệt lưới Card các `Giá`.
- **Tương tác 2 nút Số Kệ / Số Tầng trên top bar**: Bấm "Số Giá" bung tất cả Giá ra trang chủ. Bấm chữ xanh "Số Tầng", bùm, một hệ thống thống kê các Tầng trên toàn xưởng xuất hiện với màu Xanh Lá cực mát mắt. Từ Grid Lưới Nhện này, nhấp bất kỳ viên ngọc (card) nào cũng tự Jump xoáy sâu vào Giá/Tầng đó.
- **Deep Search & Tên gọi chuyên ngành**: Đổi format `Tầng X` thành ngôn ngữ Kho Bãi `棚 22-3 / Tầng 22-3`. Kéo theo đó, bạn có thể nhập "223" vào ô Search Trên Cùng, Trình lọc (vốn tìm Giá) nay đã biết bới qua cả Tầng, tóm lấy `Layer 223` giơ lên đầu danh sách bắt bạn click Jump.

## 10. Chỉnh chu Typograpphy & Hierarchy Card (V4.0 UI Revamp)
- **Thẻ Kệ Sắc Nét & Ảnh Thumbnail Tĩnh**: Card Trang Chủ của Kệ đã được đẩy sang giao diện chữ nhật, thêm ô Tròn lớn cho ID Kệ. Thumbnail giờ đây lấy được Ảnh Active chạy nền bất đồng bộ (Async Fetching). Thẻ được phân cấp thông tin rõ ràng với tiếng Nhật chuẩn `層`.
- **Thẻ Tầng Xanh Lá**: Card "Toàn bộ Tầng" được chuyển sang màu chủ đạo Emerald (Xanh Lục) tạo cảm giác thiên nhiên và tương phản nhẹ với màu nền.
- **Pills Tầng Bên Trong Kệ**: Các viên nhộng chữ in Nặng Nề ngày xưa đã bị gỡ. Nay sử dụng khung Border radius nhẹ nhàng, chữ Tiếng Nhật `金型` đứng trang trọng với màu đậm, còn phiên âm chữ Việt lùi về làm nền nhạt nhòa, đúng đẳng cấp YSDMS Next-Gen. Mọi Label đều có icon đi kèm. Màn hình chi tiết Giá thanh thoát và gọn gàng hơn.
